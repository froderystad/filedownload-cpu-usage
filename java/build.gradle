apply plugin: 'java'

// Apply the application plugin to add support for building an application
//apply plugin: 'application'

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

jar {
    manifest {
        attributes("Main-Class": "Downloader")
    }
}

//mainClassName = 'Downloader'

// Assist in finding Gradle, when installed on Mac using HomeBrew.
task getHomeDir << {
    println gradle.gradleHomeDir
}
